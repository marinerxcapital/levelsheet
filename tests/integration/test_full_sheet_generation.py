"""Placeholder full sheet generation test — completed in Phase 3–5."""

from __future__ import annotations

import pytest


@pytest.mark.skip(reason="Wired in Phase 3–5 once renderer + pipeline exist")
def test_full_sheet_generation_placeholder() -> None:
    assert False
